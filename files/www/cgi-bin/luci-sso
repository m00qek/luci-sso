#!/usr/bin/ucode
'use strict';

import * as fs from 'fs';
import { create as make_deps } from 'luci_sso.deps';
import { run } from 'luci_sso.entry';

const deps = make_deps();

run(deps, {
	getenv: getenv,
	stdout: fs.stdout,
	log:    deps.log
});
