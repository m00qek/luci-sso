name: CI (Consumer)

on:
  push:
    branches: [main, master]
    paths-ignore:
      - "**.md"
      - ".gitignore"
      - "LICENSE"
  pull_request:
    branches: [main, master]
    paths-ignore:
      - "**.md"
      - ".gitignore"
      - "LICENSE"

env:
  REGISTRY: ghcr.io
  DOCKER_SUITE: ci

jobs:
  test:
    name: Run Verification Suite
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: read
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      #      - name: Get Project Variables
      #        id: vars
      #        run: |
      #          VERSION=$(make -s -C devenv print-env VAR=SDK_VERSION)
      #          NODE_V=$(make -s -C devenv print-env VAR=NODE_VERSION)
      #          ALPINE_V=$(make -s -C devenv print-env VAR=ALPINE_VERSION)
      #          ARCH="x86-64"
      #          OWNER=$(echo "${{ github.repository_owner }}" | tr '[:upper:]' '[:lower:]')
      #
      #          echo "sdk_version=$VERSION" >> $GITHUB_OUTPUT
      #          echo "sdk_arch=$ARCH" >> $GITHUB_OUTPUT
      #          echo "node_tag=node${NODE_V}-alpine${ALPINE_V}" >> $GITHUB_OUTPUT
      #          echo "alpine_tag=alpine${ALPINE_V}" >> $GITHUB_OUTPUT
      #          echo "repo_owner=$OWNER" >> $GITHUB_OUTPUT
      #
      #      - name: Log in to the Container registry
      #        uses: docker/login-action@v3
      #        with:
      #          registry: ${{ env.REGISTRY }}
      #          username: ${{ github.actor }}
      #          password: ${{ secrets.GITHUB_TOKEN }}
      #
      #      - name: Warm Docker Cache (Pull Images)
      #        run: |
      #          IMAGE_BASE="${{ env.REGISTRY }}/${{ steps.vars.outputs.repo_owner }}/luci-sso"
      #          SDK_TAG="${{ steps.vars.outputs.sdk_arch }}-${{ steps.vars.outputs.sdk_version }}"
      #          NODE_TAG="${{ steps.vars.outputs.node_tag }}"
      #          ALPINE_TAG="${{ steps.vars.outputs.alpine_tag }}"
      #
      #          docker pull $IMAGE_BASE-sdk:$SDK_TAG
      #          docker pull $IMAGE_BASE-openwrt:$SDK_TAG
      #          docker pull $IMAGE_BASE-idp:$NODE_TAG
      #          docker pull $IMAGE_BASE-browser:$NODE_TAG
      #          docker pull $IMAGE_BASE-pki:$ALPINE_TAG
      #
      #          # Tag them locally to match exactly what docker-compose expects
      #          docker tag $IMAGE_BASE-sdk:$SDK_TAG luci-sso-sdk:$SDK_TAG
      #          docker tag $IMAGE_BASE-openwrt:$SDK_TAG luci-sso-openwrt:$SDK_TAG
      #          docker tag $IMAGE_BASE-idp:$NODE_TAG luci-sso-idp:$NODE_TAG
      #          docker tag $IMAGE_BASE-browser:$NODE_TAG luci-sso-browser:$NODE_TAG
      #          docker tag $IMAGE_BASE-pki:$ALPINE_TAG luci-sso-pki:$ALPINE_TAG

      - name: Start Environment
        run: make -sC devenv up DETACHED=true

      - name: Run Unit Tests
        run: make -sC devenv unit-test

      - name: Run E2E Tests
        run: make -sC devenv e2e-test

      - name: Stop Environment
        if: always()
        run: make -sC devenv down
